# 开发记录 & 踩坑日志

记录临渊号曙光开发过程中遇到的各类工程问题及其解决方案。

---

## 训练方案演进

### 方案一：单船时流（弃用）

**做法**：1 艘船开 10 倍时流，通讯间隔 0.02 秒。

**问题**：
- 单船单帧样本量极少，PPO 跑一整个白天（上午+下午）零提升
- 0.02 秒通讯频率对游戏其他系统延时压力极大，推理训练很不友好
- 根本原因：样本量远不满足 PPO 开始有成效的下限

### 方案二：模拟环境参数（实验性）

**做法**：提取环境参数在 Python 端模拟环境。

**发现**：
- Starsector 的位置和速度信息存在**时序不同步**问题，位置差分算出的速度与直接获取的速度不一致
- 用位置差分代替速度，可以提取出更准确的环境参数
- 以 0.006 秒步长跑 6000 轮，位置误差约 3m（35 秒积累 3m），在 Starsector 动辄上百米的量级下完全可用

**缺陷**：
- 需要手动建模所有环境交互（射线侦测、碰撞检测等）
- 凸多边形碰撞检测需要自己写 SAT（分离轴定理），工程量极大

### 方案三：并行多船（当前方案 ✅）

**做法**：生成 200 艘无碰撞虚影，用 ship ID / 序号隔离各自数据，PPO 在线训练。

**优势**：
- 不开时流，0.1 秒采集一次
- Python 端实测帧间隔约 **0.2 秒**，对模型推理控制绰绰有余
- 相比方案一有约 **20 倍** 速度提升
- 数小时即可完成上千万样本采集，PPO 样本量足够

**规模测试**：

| 舰船数 | 帧间隔 | 结论 |
|--------|--------|------|
| 200 | ~0.2s | ✅ 可用 |
| 500 | ~2s | ❌ 不可用 |
| 1000 | ~10s | ❌ 不可用，CPU + 通讯双重瓶颈 |

**当前结论**：200 船 + 0.1s 采集间隔是性价比最优的配置。

### 关于通讯协议

由于 Starsector 的沙盒安全限制，很难直接做二进制流输出，必然有 Socket 通讯瓶颈。但在 200 船环境下通讯瓶颈不明显，当前文本协议足够，暂无需优化。

---

